// Set VITE_API_URL as an environment variable in Vercel (Project Settings →
// Environment Variables) once the backend is deployed, e.g.
// https://jensi-electrical-invoice-api.onrender.com
export const API_BASE_URL = import.meta.env.VITE_API_URL || "";
